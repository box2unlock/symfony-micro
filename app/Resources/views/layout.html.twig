<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Symfony Micro Edition</title>
    <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}">
    <style>
        html,
        body
        {
            height: 100%;
        }

        body
        {
            font-size: 14px;
            font-family: Arial, sans-serif;
            line-height: 1.4285714285;
            margin: 0;
            text-align: center;
        }

        a
        {
            text-decoration: none;
            color: dodgerblue;
        }

        .app-section
        {
            padding: 20px;
        }

        header.app-section
        {
            background: #f5f5f5;
            box-shadow: inset 0 -1px 0 #eee;
        }
    </style>
</head>
<body>

    <header class="app-section">
        <strong>Symfony Micro Edition ({{ symfony_version.version }})</strong><br>
        <a href="{{ path('home') }}">Home</a> - <a href="https://github.com/box2unlock/symfony-micro" target="_blank">GitHub</a>
    </header>

    <section class="app-section">
        {% block body %}{% endblock %}
    </section>

</body>
</html>
